language: generic

sudo: required

services:
  - docker

script:
  - bash ci.sh

branches:
  only:
    - auto
    - master

notifications:
  email:
    on_success: never
  webhooks: http://japaric-homu.herokuapp.com/travis
